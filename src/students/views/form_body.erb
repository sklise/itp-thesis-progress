<div class="meta-form-elements">
<h6>Category</h6>
<fieldset class="button-labels">
  <% @categories.each do |category| %>
  <input type="radio" value="<%= category.id %>" name="post[category_id]" id="category-<%= category.id %>" <%= "checked" if @post.category == category %>>
  <label for="category-<%= category.id %>"><%= category.name %></label>
  <% end %>
</fieldset>

<h6>Assignments</h6>
<fieldset class="button-labels">

  <% if @post.assignment %>

  <input class="assignment-input"  type="radio" checked value="<%= @post.assignment_id %>" name="post[assignment_id]" id="assignment-<%= @post.assignment_id %>" data-brief="<%= @post.assignment.brief %>">
  <label for="assignment-<%= @post.assignment_id %>">
    <%= @post.assignment.title %>
  </label>

  <% end %>

  <input class="null-radio" value="" name="post[assignment_id]" type="radio">
  <% @current_user.open_assignments.each do |assignment| %>

    <input class="assignment-input" type="radio" value="<%= assignment.id %>" name="post[assignment_id]" id="assignment-<%= assignment.id %>" <%= "checked" if @assignment &&  assignment.id.to_i == (@assignment.id || "").to_i %> data-brief="<%= assignment.brief %>">

    <label <%= "class=checked" if @assignment && assignment.id.to_i == (@assignment.id || "").to_i %> for="assignment-<%= assignment.id %>"><%= assignment.title %></label>
  <% end %>

</fieldset>
<div class="assignment-brief" ><%= "<h6>Brief</h6>"+@assignment.brief if @assignment %></div>
</div>

<h6>Public/Private</h6>
<p>Your posts require visitors to be logged in to view by default. Use this option to allow individual posts to be accessible by anyone on the internet. Public posts will not be indexed on search engines. Comments on posts are only viewable to logged in users.</p>
<fieldset class="button-labels">
  <% if @post.is_public == true %>
  <input type="checkbox" value="false" name="post[is_public]" id="post-public">
  <label for="post-public">
    Hide this post from the internet.
  </label>
  <% else %>
  <input type="checkbox" value="true" name="post[is_public]" id="post-public">
  <label for="post-public">
    Make this post viewable to the entire internet.
  </label>
  <% end %>
</fieldset>

<div id="post-submit" class="action-buttons">
  <button id="private" name="post[draft]" value="true">Draft</button>
  <button id="public" name="post[draft]" value="false">Publish</button>
</div>