<div class="post">
  <hgroup>
  <h1><a href="<%= post.url %>"><%= post.title %></a></h1>
  <h3 class="date"><%= post.published_at.strftime("%B %d") %></h3>
  <h4 class="category"><%= post.category %></h4>
  </hgroup>

  <div class="post-content">
    <%= mdown post.content %>
  </div>
</div>
